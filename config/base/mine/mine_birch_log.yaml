defaults:
  - base
  - _self_

candidate_preferred_spawn_biome:
  - "forest"

task_conf:
  - name: "mine"
    text: "mine_block:birch_log"

reward_conf:
  - event: "mine_block"
    max_reward_times: 1
    reward: 1.0
    objects:
      - "birch_log"
    identity: "mine birch_log"

init_inventory:
  []
