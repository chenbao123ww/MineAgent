defaults:
  - base
  - _self_

candidate_preferred_spawn_biome:
  - "plains"

task_conf:
  - name: "mine"
    text: "mine_block:dirt"

reward_conf:
  - event: "mine_block"
    max_reward_times: 1
    reward: 1.0
    objects:
      - "dirt"
    identity: "mine dirt"

init_inventory:
  []
