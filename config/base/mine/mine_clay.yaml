defaults:
  - base
  - _self_

candidate_preferred_spawn_biome:
  - "swamp"

task_conf:
  - name: "mine"
    text: "mine_block:clay"

reward_conf:
  - event: "mine_block"
    max_reward_times: 1
    reward: 1.0
    objects:
      - "clay"
    identity: "mine clay"

init_inventory:
  []
