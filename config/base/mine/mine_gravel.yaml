defaults:
  - base
  - _self_

candidate_preferred_spawn_biome:
  - "beach"

task_conf:
  - name: "mine"
    text: "mine_block:gravel"

reward_conf:
  - event: "mine_block"
    max_reward_times: 1
    reward: 1.0
    objects:
      - "gravel"
    identity: "mine gravel"

init_inventory:
  []
